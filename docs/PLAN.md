# Mixion — MVP 設計提案（ウェブアプリ版）

作成日: 2026-09-05（Tauri 版から改訂）
対象: 実写動画をフレーム単位で印刷 → 手作業加工 → スキャン → 動画に戻す制作フローの「面倒な部分だけ」を自動化する小さなツール。

---

## 0. 要件の整理

### 確定していること
- 4ステップの一直線フロー: **Print → Draw → Scan → Animate**
- 入力: 動画ファイル（D&D）、FPS（6 / 8 / 12 / 任意）、1ページあたりのフレーム数（2×2 / 3×3 / 4×3）
- 出力: 印刷用PDF（A4）、最終動画（MP4 / GIF）、可能なら元音声を再結合
- スキャン側 v0.1 は「ユーザーが四隅をクリック → パース補正 → グリッド分割」で可
- 完全ローカル処理。外部サーバーへの送信なし
- **ウェブアプリ。サーバー・データベース・アカウントは持たない**（会話で確定）
- 技術: React / TypeScript / Canvas / PDF生成ライブラリ、必要ならOpenCV
- 最初のゴール: 5秒動画 → 8fps → 40フレーム → A4×10枚（2×2）→ 印刷 → 手描き → スキャン → 40フレーム復元 → MP4

### 「ウェブアプリ」の意味
- Vite でビルドした静的ファイルだけで動く。`npm run dev` で手元で開くか、任意の静的ホスティングに置く
- 動画もスキャン画像もブラウザの外に出ない。ネットワーク通信は最初のページ読み込みだけ
- Tauri / Rust / FFmpeg のインストールは不要

### 状態を持たない設計（MVPの方針）
印刷とスキャンの間には数日空くのが普通なので、「何かを保存しておく」設計にすると保存場所の問題が出る。代わりに**紙自体に設定を持たせる**。

- 各ページの QR に、レイアウトを再構築するのに必要な情報をすべて入れる（fps、グリッド、ページ番号、総ページ数、フレーム範囲、総フレーム数、元動画の縦横サイズ、プロジェクトID）
- スキャン側は最初の 1 枚の QR を読むだけで設定を復元する。QR が読めなければ手入力する（項目は 4 つだけ）
- 元動画は Scan 側でもう一度ドロップしてもらう。音声と、未スキャンフレームの代用に使う。ドロップしなければ無音・代用なしで進む
- Print 画面と Scan 画面は完全に独立して動く。リロードすれば消えるが、消えて困るものは紙と元動画に残っている
- ブラウザ内保存（IndexedDB）は「作業途中の四隅クリックをリロードで失いたくない」と感じてから足す

### こちらで置いた解釈・仮定
| 項目 | 仮定 |
|---|---|
| 「切り取り位置ガイド」 | 物理的にハサミで切る線ではなく、「最終動画に入る範囲（クロップ枠）」の目印。ページを切ると四隅マーカーが失われるため、**ページは切らずにスキャン**する |
| スキャン入力形式 | JPEG / PNG（1ページ1ファイル）。PDF はMVPでは非対応 |
| スキャン機材 | フラットベッドスキャナ想定。スマホ撮影もパース補正で吸収できるが、照明ムラは補正しない |
| 用紙 | A4のみ。向きは元動画のアスペクト比とグリッドから自動決定 |
| 出力解像度 | 元動画と同じ解像度（上限 1920px幅）。QR に入れた縦横サイズから決まる |
| 未スキャンのフレーム | エラーにせず、元動画のフレームで代用して警告表示。元動画がなければ直前のフレームを保持 |
| 対応ブラウザ | **まず Chrome / Edge**。WebCodecs と File System Access API が揃っている。Safari は後で確認 |
| 保存 | ブラウザのダウンロード。Chrome 系では保存先を選べる |

### 設計上の最重要ポイント
1. **レイアウト計算を純粋TS関数1つに集約し、PDF生成とスキャン分割が同じmm座標を使う。** ここがズレると全部ズレる。
2. **切り出しは四隅マーカーからの相対座標で行う。** 既知のmm位置とスキャン上の4点のホモグラフィで、印刷時の拡大縮小やスキャン解像度の違いを吸収する。「100%で印刷して」と要求しない。
3. **QR にレイアウトの入力値をすべて入れる。** `computeLayout(QRの中身)` でスキャン側が同じレイアウトを再構築できる。これが「状態を持たない」を成立させる。
4. **v0.2 以降の自動化は「4点の座標がクリック由来かマーカー検出由来か」の差だけ。** 後段のパイプラインは最初から共通。

---

## 1. MVP 仕様

### 1.1 Print
- 動画ファイルを D&D またはファイル選択で読み込む（mp4 / mov / webm）
- 読み込み時に WebCodecs で取得: 長さ、解像度、音声有無 → 表示
- FPS: `6` `8` `12` のチップ + 任意入力（1〜30 の整数）
- グリッド: `2×2` `3×3` `4×3`（列×行）。既定は 4×3（A4 横 4 枚で 40 コマ）
- 自動計算して表示: 総フレーム数 = ceil(長さ × fps)、ページ数 = ceil(フレーム数 / 1ページのフレーム数)、用紙の向き
  例: 5.0秒 × 8fps = 40フレーム → 2×2 → 10ページ（A4 横）
- ページプレビュー（Canvas、全ページを縦に一覧。画面に近づいたページから順にフレームを取り出して描く）。PDF と同じレイアウト関数・同じ描画関数を使う
- 「Create Print PDF」でフレーム抽出 → PDF 生成 → ダウンロード（Chrome 系は保存先を選択）
- フレーム抽出: t = i / fps（i = 0 .. N-1）の各時刻に最も近いフレームを取る。N = ceil(長さ × fps) を正とし、動画末尾で取れなければ最終フレームを複製

### 1.2 印刷ページのフォーマット（A4）
```
┌──────────────────────────────────────────────┐  ← 用紙端
│ ■M0            P03/10  #09–#12  [QR]     M1■ │  ← 外周余白10mm、マーカー10mm角
│                                              │
│   ┌──────────────┐    ┌──────────────┐       │
│   │  frame #09   │    │  frame #10   │       │  ← 画像。四隅に細いクロップ目印
│   └──────────────┘    └──────────────┘       │
│     #09                 #10                  │  ← フレーム番号
│   ┌──────────────┐    ┌──────────────┐       │
│   │  frame #11   │    │  frame #12   │       │
│   └──────────────┘    └──────────────┘       │
│     #11                 #12                  │
│ ■M3                                      M2■ │
└──────────────────────────────────────────────┘
```
| 要素 | 仕様 |
|---|---|
| 外周余白 | 10mm。プリンタの印刷不可領域とスキャナの端欠けを避ける |
| 四隅マーカー | ArUco系マーカー 10mm角 + 白の余白2mm。ID = `page*4 + corner`（0=左上,1=右上,2=右下,3=左下）。1個読めればページと向きが分かる（v0.2 以降で使用。v0.1 では黒い■として見えていればよい） |
| ページ番号 | ヘッダに `P03 / 10` とフレーム範囲 |
| QR | ヘッダ右、16mm。内容は下記 |
| フレーム画像 | セルの最大枠にアスペクト維持でフィット。JPEG 品質 85 |
| クロップ目印 | 画像枠の1.5mm内側にコーナーティック。紙の白が動画に入らないためのブリード |
| フレーム番号 | 各画像の直下に `#09` |
| セル間隔 | 6mm |
| 向き | 横 / 縦のうちセル面積が大きくなる方 |

QR の内容（短い JSON。v が layout アルゴリズムのバージョン）:
```json
{"v":1,"p":"k7Qz","pg":3,"of":10,"f":[9,12],"n":40,"fps":8,"g":"2x2","d":[1920,1080]}
```

参考サイズ（A4横、16:9、layout.ts の実測）: 2×2 → 約123×69mm、3×3 → 約75×42mm、4×3 → 約65×36mm

### 1.3 Draw（説明のみ）
- 四隅の■と QR の上は塗らない・貼らない（塗ってしまっても四隅を手動指定すれば復元できる）
- 画像の外にはみ出して描いてよい。クロップ枠の外は動画に入らない
- 厚みのあるコラージュはフラットベッドで
- スキャン設定: 300dpi、カラー、JPEG か PNG、1ページ1ファイル
- ページを切り離さない

### 1.4 Scan
- 「Import Scans」（複数ファイル選択 / D&D）
- 最初の 1 枚から QR を読んで設定を復元し、画面上部に表示（`8fps · 2×2 · 10 pages · 1920×1080`）。読めなければ設定フォームを出す
- 以降の各スキャンも QR を試み、ページ番号を自動入力。読めなければ取り込み順を初期値にして手動選択
- 別プロジェクトの QR（p が違う）が混ざったら警告
- 「Original video（任意）」のドロップ欄。音声と代用フレーム用
- 左: スキャン一覧。状態は `四隅未指定` / `OK` / `エラー`
- 中央: 四隅指定キャンバス
  - 左上 → 右上 → 右下 → 左下 の順に 4 点クリック（マーカーの中心）
  - 4 点そろった時点でグリッドの予測枠をオーバーレイ表示し、点をドラッグで微調整できる
  - 拡大鏡
- 「Apply」でホモグラフィ計算 → セルごとに切り出し → メモリ上の出力フレームに登録

### 1.5 Animate（Scan 画面の右パネル）
- フレームストリップ: 1〜N のサムネイル。スキャン由来は通常、元動画代用は黄、直前フレーム保持は赤
- プレビュー再生（指定 fps、ループ）
- 「Export MP4」「Export GIF」、音声 ON/OFF（元動画がドロップされていて音声がある場合のみ）
- MP4: WebCodecs の VideoEncoder（H.264）+ MP4 ムクサー。各フレームの表示時間 = 1/fps。音声は元動画のトラックを再エンコードせずコピー
- GIF: 純 JS のエンコーダ、幅 640px（設定可）
- 保存はダウンロード。Chrome 系では `showSaveFilePicker` で保存先を選択

---

## 2. アーキテクチャ

```
ブラウザ（Chrome / Edge）
┌────────────────────────────────────────────────────────────────┐
│ React + TS (Vite)                                              │
│                                                                │
│  UI: Print / Draw / Scan+Animate     state: zustand（メモリのみ）│
│                                                                │
│  domain/  layout.ts  frameMap.ts  homography.ts  markers.ts     │
│           ← 純粋TS。DOM 非依存。PDF と Scan の共通基盤             │
│                                                                │
│  lib/video/  decode.ts (WebCodecs)   encode.ts (WebCodecs+MP4)  │
│              gif.ts                   [fallback: ffmpeg.wasm]    │
│  lib/pdf/    pdf-lib + qrcode                                   │
│  lib/files/  D&D, download, showSaveFilePicker                  │
│  workers/    warp.worker.ts（逆写像 + バイリニア）               │
└────────────────────────────────────────────────────────────────┘
        ▲ ファイル D&D                    ▼ PDF / MP4 / GIF ダウンロード
   ネットワーク通信なし。サーバー・DB・アカウントなし
```

### 役割分担
- **状態はメモリだけ。** Print と Scan は独立したモードで、共有するのは紙（QR）だけ
- **動画の入出力は WebCodecs。** デコード（フレーム抽出）とエンコード（MP4）をブラウザ標準 API で行う。読み込み・ムクシング・音声トラックのコピーは mediabunny のようなライブラリに任せる（対応範囲は実装時に要確認）
- **ffmpeg.wasm は保険。** WebCodecs で扱えない入力形式や音声コピーがうまくいかない場合のフォールバック。約 30MB の読み込みと COOP/COEP ヘッダが必要なので、使わずに済めば使わない
- **OpenCV は使わない。** 4 点ホモグラフィは 8×8 の連立方程式を自前で解く。ワープは Web Worker で逆写像 + バイリニア（1 セル 1920×1080 で数百 ms）
- **重い処理は Worker。** ワープ、GIF エンコード、（必要なら）デコードもメインスレッドから外す

---

## 3. 使用ライブラリ

| 領域 | 採用 | 備考 |
|---|---|---|
| UI | React 19 + TypeScript + Vite | |
| スタイル | Tailwind CSS | 画面 3 枚なので最小限 |
| 状態 | zustand | メモリのみ。永続化しない |
| スキーマ | zod | QR ペイロードと手入力設定のバリデーション |
| PDF | pdf-lib | ベクタ描画 + JPEG 埋め込み |
| QR 生成 | qrcode | Canvas / データURL → pdf-lib に埋め込み |
| QR 読取 | jsQR | 純 JS |
| 動画デコード / エンコード / MP4 | WebCodecs + mediabunny（候補） | 時刻指定でのフレーム取得、H.264 エンコード、音声トラックのコピー。要検証 |
| 動画フォールバック | @ffmpeg/ffmpeg（ffmpeg.wasm） | 必要になったときだけ導入 |
| GIF | gifenc | 純 JS、軽量 |
| ArUco（v0.2〜） | js-aruco2 | 純 JS。対応辞書は要確認 |
| ホモグラフィ / ワープ | 自前実装 | 依存を増やさない |
| テスト | vitest | domain/ の純粋関数中心 |
| Lint | oxlint（Vite テンプレート同梱） | |

---

## 4. データフロー

### Print
```
動画ファイル (File)
 → decode.ts: メタデータ {duration, width, height, hasAudio}
 → UI で fps / grid 選択
 → settings = {fps, grid, dims}  → computeLayout(settings) → Layout (mm)
 → N = ceil(duration*fps), pages = ceil(N/perPage), projectId = ランダム4文字
 → decode.ts: t_i = i/fps の各フレームを Canvas に描画 → JPEG Blob × N（メモリ）
 → pagePainter で各ページを描画（Canvas プレビュー / pdf-lib 出力）
     マーカー4 / ヘッダ / QR{v,p,pg,of,f,n,fps,g,d} / 画像 / クロップ目印 / 番号
 → pages.pdf を Blob 化 → ダウンロード or showSaveFilePicker
 → 状態は破棄してよい
```

### Scan → Animate
```
スキャン画像 (File[])  [+ 元動画 (File, 任意)]
 → 各画像: デコード → jsQR → payload {v,p,pg,...}
     最初に読めた payload から settings を復元 → computeLayout(settings)
     読めない画像はページ番号 = 取り込み順（手動で変更可）
 → (v0.1) ユーザーが 4 隅クリック / (v0.2+) マーカー検出
 → H = solveHomography(layout.markerCenters[mm] → points[px])
 → 各セル c: 出力画像 (dims) の各ピクセル
        → cropRect 内の mm 座標 → H → スキャン px → バイリニア   (Worker)
     → outputFrames[frame(pg,c)] = ImageBitmap / Blob
 → 未割当フレーム: 元動画があれば t_i のフレームを decode.ts で取得、なければ直前を保持
 → encode.ts: フレーム列 (duration 1/fps) + 音声トラック(コピー) → MP4 Blob
    gif.ts:   フレーム列 → GIF Blob
 → ダウンロード or showSaveFilePicker
```

### フレーム ↔ ページ ↔ セル（1-origin）
```
perPage    = cols * rows
page(f)    = floor((f-1) / perPage) + 1
cell(f)    = (f-1) mod perPage           // 左→右、上→下
frame(p,c) = (p-1)*perPage + c + 1
```

---

## 5. 画面構成

上部に `Print → Draw → Scan → Animate` のステップ表示。実体は 3 画面（Draw は静的、Animate は Scan の右パネル）。Home 画面は不要（開いたら Print）。

### 5.1 Print
```
┌──────────────────────────────────────────────────────────────┐
│ ● Print   ○ Draw   ○ Scan   ○ Animate                        │
├──────────────────────────┬───────────────────────────────────┤
│  ┌────────────────────┐  │  Page Preview      ◀ 1 / 10 ▶     │
│  │  Drop Video Here   │  │  ┌───────────────────────────┐    │
│  └────────────────────┘  │  │  ■  P01/10  #01–#04 [QR] ■│    │
│  walk.mov 1920×1080 5.0s │  │   [img]  [img]            │    │
│  audio ✓                 │  │   [img]  [img]            │    │
│  FPS   [6] [8] [12] [__] │  │  ■                       ■│    │
│  Grid  [2×2] [3×3] [4×3] │  └───────────────────────────┘    │
│  40 frames → 10 pages    │                                   │
│  A4 landscape            │                                   │
│  [ Create Print PDF ]    │                                   │
│  ▓▓▓▓▓░░░░ extracting…   │                                   │
└──────────────────────────┴───────────────────────────────────┘
```

### 5.2 Draw
チェックリスト + 「Next: Scan」。

### 5.3 Scan / Animate
```
┌──────────────────────────────────────────────────────────────────────┐
│ ○ Print   ○ Draw   ● Scan   ○ Animate                                │
│ Project k7Qz · 8fps · 2×2 · 10 pages · 1920×1080   [Original video ▢]│
├──────────────┬────────────────────────────────┬──────────────────────┤
│ [Import Scans]│  scan003.jpg      Page [ 3 ▾ ] │ Frames  38/40        │
│ scan001  OK  │  ┌──────────────────────────┐  │ ┌──┬──┬──┬──┬──┬──┐ │
│ scan002  OK  │  │  (スキャン + 4点 + 予測枠)  │  │ │01│02│03│04│05│06│ │
│ scan003  ●   │  │                          │  │ ├──┼──┼──┼──┼──┼──┤ │
│ scan004  ?   │  └──────────────────────────┘  │ │..│..│!!│..│..│..│ │
│ scan005  ✗   │  TL → TR → BR → BL             │ └──┴──┴──┴──┴──┴──┘ │
│              │  [Reset]  [Apply]              │ ┌──────────────────┐ │
│              │                                │ │  ▶ preview  8fps │ │
│              │                                │ └──────────────────┘ │
│              │                                │ audio [✓]            │
│              │                                │ [Export MP4] [GIF]   │
└──────────────┴────────────────────────────────┴──────────────────────┘
```

---

## 6. ディレクトリ構成

```
mixion/
├── package.json
├── vite.config.ts
├── tsconfig.json
├── .oxlintrc.json
├── index.html
├── src/
│   ├── main.tsx
│   ├── App.tsx                # ステップ切替（ルータ不要）
│   ├── app/
│   │   ├── store.ts           # zustand。print / scan の状態（メモリのみ）
│   │   └── Steps.tsx
│   ├── domain/                # 純粋 TS。DOM 非依存。vitest 対象
│   │   ├── layout.ts          # ★ レイアウト計算（mm）
│   │   ├── frameMap.ts        # frame ↔ page/cell
│   │   ├── homography.ts      # 4点 DLT、逆行列、変換
│   │   ├── markers.ts         # ArUco 辞書ビット + ID 割当
│   │   └── settings.ts        # QR ペイロードの zod スキーマ、encode/decode
│   ├── features/
│   │   ├── print/
│   │   │   ├── PrintStep.tsx
│   │   │   ├── VideoDrop.tsx
│   │   │   ├── PagePreview.tsx
│   │   │   ├── pagePainter.ts # Canvas と pdf-lib の両方に描く抽象
│   │   │   └── buildPdf.ts
│   │   ├── draw/DrawStep.tsx
│   │   ├── scan/
│   │   │   ├── ScanStep.tsx
│   │   │   ├── ScanList.tsx
│   │   │   ├── SettingsBar.tsx    # QR 復元結果 / 手入力フォーム
│   │   │   ├── CornerPicker.tsx
│   │   │   ├── ScanProgress.tsx   # 切り出し済み数と未取得ページ
│   │   │   ├── slicer.ts          # Worker 呼び出し、セル → 出力フレーム
│   │   │   └── qrPage.ts          # jsQR
│   │   └── animate/
│   │       ├── FrameStrip.tsx     # 全コマのサムネイル。クリックで Player がそのコマへ
│   │       ├── Player.tsx
│   │       └── ExportPanel.tsx
│   ├── lib/
│   │   ├── video/
│   │   │   ├── decode.ts      # メタデータ、t_i のフレーム取得（WebCodecs）
│   │   │   ├── encode.ts      # MP4（H.264 + 音声コピー）
│   │   │   └── gif.ts
│   │   ├── files.ts           # D&D、download、showSaveFilePicker
│   │   └── image.ts           # decode、ImageData、toBlob
│   ├── workers/
│   │   └── warp.worker.ts
│   └── components/ui/
├── fixtures/
│   ├── sample-5s.mp4
│   └── scans/                 # 実機スキャン（回帰テスト用）
└── tests/
    ├── layout.test.ts
    ├── frameMap.test.ts
    ├── homography.test.ts
    └── settings.test.ts
```

---

## 7. 実装順序

「ソフトの前に紙を 1 枚通す」を早い段階に入れる。マーカーサイズ、余白、スキャナの端欠けは実機でしか分からない。

| 順 | 内容 | ゴール |
|---|---|---|
| 1 | Vite + React + TS、oxlint、vitest のスキャフォールド | 土台 |
| 2 | `domain/layout.ts` + `frameMap.ts` + `settings.ts` + テスト | 数値の唯一の源 |
| 3 | `pagePainter.ts` + `buildPdf.ts`。ダミー画像で 10 ページ PDF を出す（UI なしでよい） | **実機テスト1: 印刷 → 落書き → スキャン**。layout の定数を確定 |
| 4 | **WebCodecs の検証スパイク**: 5 秒動画から t_i のフレーム 40 枚取得、H.264 で MP4 出力、音声コピー。ここで mediabunny の採否と ffmpeg.wasm の要否を決める | 最大の技術リスクを早く潰す |
| 5 | Print 画面: D&D、メタデータ表示、FPS / Grid、枚数表示、PagePreview | |
| 6 | フレーム抽出 → PDF → ダウンロード（進捗付き） | **Print 完了** |
| 7 | `homography.ts` + テスト。`warp.worker.ts` + `slicer.ts`。実機テスト1のスキャンに手打ち座標で 4 フレーム切り出し | パイプラインの核 |
| 8 | Scan 画面: Import、QR で設定復元、手入力フォーム、一覧、CornerPicker、ページ番号、Apply | **Scan 完了（v0.1）** |
| 9 | 元動画ドロップ、未割当フレームの代用、FrameStrip、Player | Animate の可視化 |
| 10 | MP4 書き出し（音声）、GIF 書き出し、保存 | **Animate 完了** |
| 11 | **実機テスト2: 5 秒動画で最初から最後まで通す** | 最初のゴール |
| 12 | README、fixtures | 再現可能に |

---

## 8. GitHub Issue 一覧

Milestone: `M1 Print` / `M2 Scan` / `M3 Animate` / `M4 E2E`。Size: S(半日) / M(1日) / L(2〜3日)。

| # | タイトル | MS | Size | 依存 |
|---|---|---|---|---|
| 1 | Vite + React + TS + Tailwind + oxlint + vitest のスキャフォールド | M1 | S | - |
| 2 | `domain/layout.ts`: A4・向き・グリッド・アスペクトからマーカー / QR / セル / クロップ枠の mm 座標 + テスト | M1 | M | - |
| 3 | `domain/frameMap.ts`: frame ↔ page/cell + テスト | M1 | S | 2 |
| 4 | `domain/settings.ts`: QR ペイロードの zod スキーマ、encode / decode、projectId 生成 + テスト | M1 | S | - |
| 5 | `domain/markers.ts`: ArUco 辞書ビットの同梱と ID 割当（v0.1 では描画のみ） | M1 | M | - |
| 6 | `pagePainter.ts`: Canvas と pdf-lib に同じ描画コマンドで描く抽象 | M1 | M | 2,4,5 |
| 7 | `buildPdf.ts`: pdf-lib で全ページ PDF（マーカー・ヘッダ・QR・画像・クロップ目印・番号） | M1 | M | 6 |
| 8 | 【実機】ダミー画像の PDF を印刷 → 落書き → スキャンし、余白 / マーカー / 端欠けを検証して layout 定数を確定 | M1 | S | 7 |
| 9 | 【スパイク】WebCodecs 検証: メタデータ取得、t_i のフレーム 40 枚取得、H.264 MP4 出力、音声トラックコピー。mediabunny 採否と ffmpeg.wasm 要否を決める | M1 | L | 1 |
| 10 | `lib/video/decode.ts`: メタデータと時刻指定フレーム取得（スパイク結果を実装に） | M1 | M | 9 |
| 11 | Print 画面: 動画 D&D / 選択、メタデータ表示、FPS チップ + 任意入力、グリッド、枚数 / 向き表示 | M1 | M | 10 |
| 12 | PagePreview（Canvas、ページ送り） | M1 | S | 6,11 |
| 13 | 「Create Print PDF」: 抽出 → PDF → ダウンロード / showSaveFilePicker、進捗 | M1 | M | 7,12 |
| 14 | `domain/homography.ts`: 4点 DLT、逆行列、変換 + テスト | M2 | M | - |
| 15 | `warp.worker.ts` + `slicer.ts`: セル → 出力解像度の逆写像バイリニア（Worker） | M2 | M | 2,14 |
| 16 | Scan 取り込み: 複数選択 / D&D、自然順ソート、一覧と状態 | M2 | M | 1 |
| 17 | QR による設定復元とページ番号自動入力（jsQR）、手入力フォーム、別プロジェクト混入の警告 | M2 | M | 4,16 |
| 18 | CornerPicker: 4点クリック、ドラッグ微調整、拡大鏡、予測グリッド overlay | M2 | L | 14,16 |
| 19 | ページ番号選択と Apply: 切り出し → outputFrames 登録 | M2 | M | 15,17,18 |
| 20 | 元動画ドロップと未割当フレームの代用（元動画なしは直前フレーム保持）、警告表示 | M3 | M | 10,19 |
| 21 | FrameStrip（色分け）と Player（fps ループ再生） | M3 | M | 20 |
| 22 | `lib/video/encode.ts`: H.264 MP4、フレーム duration 1/fps、音声コピー、進捗 | M3 | M | 9,20 |
| 23 | `lib/video/gif.ts`: gifenc、幅指定（Worker） | M3 | S | 20 |
| 24 | ExportPanel: MP4 / GIF、音声トグル、保存 | M3 | S | 22,23 |
| 25 | 【実機】5 秒動画で Print → Draw → Scan → Animate を通し、フレーム数と音ズレを確認 | M4 | M | 13,24 |
| 26 | fixtures と README（対応ブラウザ、セットアップ） | M4 | S | 25 |
| 27 | エラーハンドリング: 非対応コーデック、壊れた画像、QR 不整合、メモリ超過時の案内 | M4 | M | 25 |
| 28 | リロード前の確認ダイアログ（beforeunload。作業中の四隅指定を守る） | M4 | S | 19 |

---

## 9. 技術的に難しい部分・リスク

| リスク | 影響 | 対策 |
|---|---|---|
| **WebCodecs のブラウザ差**（Safari の H.264 エンコード対応、Firefox の対応状況） | 特定ブラウザで動かない | MVP は Chrome / Edge を対象と明記。Issue 9 のスパイクで確定。最終手段は ffmpeg.wasm |
| **音声トラックのコピー**（AAC の再エンコードはブラウザで制限がある） | 音声なしになる | 再エンコードせず元のパケットをそのまま MP4 に載せる。ライブラリが対応していなければ ffmpeg.wasm で結合 |
| **時刻指定のフレーム取得の正確さ**（キーフレーム間隔、VFR 動画） | 40 枚のはずが 39 / 41 枚、フレームのズレ | N = ceil(duration × fps) を正として、各 t_i に最も近いフレームを取る。末尾は複製。実機テスト2で確認 |
| **印刷時のスケーリング / スキャン解像度** | 切り出し位置ズレ | マーカー相対座標 + ホモグラフィで吸収 |
| **絵の具・コラージュがマーカーや QR を隠す** | QR が読めない、自動検出不能 | 設定は手入力で代替できる（4 項目）。四隅は手動指定。Draw 画面で注意喚起 |
| **端欠け・印刷不可領域** | マーカー欠け | 10mm 余白。実機テスト1で確定 |
| **メモリ**（A4 300dpi ≒ 2500×3500px、フレーム 40 枚、出力フレーム 40 枚） | タブがクラッシュ | スキャンは 1 枚ずつ処理して元画像を捨てる。フレームは JPEG Blob で保持し、必要時だけ ImageBitmap 化 |
| **リロードで作業が消える** | 四隅指定のやり直し | 状態を持たない設計なので被害は「そのセッションの四隅クリック」だけ。beforeunload で確認。必要なら後から IndexedDB |
| **保存 UX**（Safari はダウンロードのみ、複数ファイルの扱い） | 保存先が分かりにくい | Chrome 系では showSaveFilePicker。出力は PDF / MP4 / GIF の単一ファイルなので許容 |
| **ffmpeg.wasm を使う場合の COOP / COEP ヘッダ** | ホスティングで設定が要る | 使わずに済ませる。使うなら Vite dev と静的ホスティングでヘッダを設定 |
| **紙の白と動画の白、色味の差** | 眠い仕上がり | MVP ではやらない。1.5mm ブリードで紙端の写り込みだけ防ぐ |
| **js-aruco2 の対応辞書と精度（v0.2）** | 自動検出が不安定 | 手動が本線。ダメなら OpenCV.js に切替できる構造 |

---

## 10. MVP では後回しにすべき機能

### 後回し（ロードマップ上）
- 四隅マーカーの自動検出（v0.2）
- 回転・傾き・パースの自動補正（v0.3。マーカーが取れれば v0.2 とほぼ同時）
- ページ番号・フレーム番号の自動判定（v0.4。QR は v0.1 に前倒し済み、マーカー ID でも可能）
- ブラウザ内保存（IndexedDB）と作業の再開
- スキャナの PDF 出力・複数ページ PDF の取り込み
- Safari / Firefox 対応の確認と対応
- 実スキャンをフィクスチャにした回帰テスト
- PWA 化（オフラインで開ける）

### やらない・ずっと後
- 動画のトリミング、区間指定、クロップ、アスペクト変更
- タイムライン編集、並べ替え、フレームホールド、タイミング調整
- オニオンスキン、ライトテーブル
- 色補正、レベル補正、ゴミ取り、紙の白飛ばし
- A4 以外の用紙、両面印刷
- 複数プロジェクト、Undo
- 書き出しプリセット、透過 PNG 連番
- デスクトップアプリ化（Tauri）。必要になったら同じコードを包むだけ
- i18n
- サーバー・DB・アカウント・共有（方針としてやらない）

---

## 実装状況（2026-09-06 時点）

| Issue | 状態 | メモ |
|---|---|---|
| 1〜7 | 完了 | スキャフォールド、domain 層、マーカー、ページ描画、PDF |
| 8 | 完了（2026-09-06 実機） | 実際に印刷・落書き・スキャンして通した。縦向きスキャンで向きが安定しない問題が出て、下記 v0.2.1 で対応 |
| 9〜13 | 完了 | WebCodecs（mediabunny）で 40 フレーム抽出 0.35s、Print 画面、PDF 保存 |
| 14〜19 | 完了 | ホモグラフィ、ワープ（Worker）、Scan 画面（QR 復元、四隅クリック、Apply） |
| 20〜24 | 完了 | 元動画の補完、Player、MP4（音声コピー）/ GIF 書き出し |
| 25 | 完了（2026-09-06 実機） | 実際の印刷 → スキャン → 切り出し → 書き出しまで通った |
| 26 | ほぼ完了 | README、`fixtures/sample-5s.mp4`。実スキャンのフィクスチャは 25 の後に追加 |
| 27 | 一部 | 非対応コーデック・QR 不整合・別プロジェクト混入は表示する。メモリ超過の案内は未 |
| 28 | 完了 | beforeunload |

### 実機テストで見るポイント
- スキャナの端で四隅マーカーが欠けないか（欠けるなら `LAYOUT_CONSTANTS.outerMargin` を増やす）
- 300dpi スキャンで QR が読めるか（読めないなら `qrSize` を大きくする）
- 「用紙に合わせる」で印刷しても Apply 後の切り出しがずれないか
- 絵の具で紙が波打った場合の位置ズレ（v0.2 以降の課題として記録）

### 既知の小さな課題
- ~~本番ビルドに `?spike=video` のページと `fixtures/sample-5s.mp4` が含まれる~~ → spike を `import.meta.env.DEV` 下の動的 import にして除外済み。デモ用の `public/sample.mp4` は意図して同梱
- Safari / Firefox は未確認（レスポンシブは 320〜1100 px で確認済み。実機のタッチ操作は未確認）

### サンプルで試す（2026-09-06 追加）
- `src/app/demo.ts`: `public/sample.mp4`（`fixtures/sample-5s.mp4` と同じ testsrc のカラーバー。フレームカウンターが動くので fps の効き方が見える）を読み込み、`renderPageToBlob` で印刷ページを 150 dpi・微小回転つきの画像にして `importScans` に通す。本物のスキャン経路（QR → マーカー検出 → ワープ）をそのまま使うので、デモがそのままパイプラインの動作確認になる
- 入口は Print の空状態（動画なしで Animate まで）と Scan の空状態（動画はあるが紙がない）

### v0.2（2026-09-05 追加）: 四隅マーカーの自動検出
- 方式: jsQR が返す QR の四隅座標から、スキャンの px/mm と回転を求め（相似変換）、レイアウト上のマーカー位置を予測。予測点の周辺だけを Otsu で二値化し、連結成分からマーカーサイズの黒い正方形を選んで外接矩形の中心を取る。ArUco の ID 復号は使っていない（QR がある限り不要）
- 取り込み時に検出 → 4 点そろえば自動で Apply。足りない隅だけユーザーがクリックする。見つかった点はドラッグで修正可能
- 疑似スキャン（200dpi、±2.5 度回転）10 ページで 9 ページが自動、塗りつぶしたマーカーのあるページは 3 点検出 + 手動 1 点。検出誤差は 1.1 px 以下（約 0.14mm）。10 ページの取り込み〜切り出しで約 13 秒
- ~~未対応: QR も塗りつぶされたページ~~ → v0.2.1 でマーカーの解読により向きとページ番号は復元できる。設定だけは他ページの QR か手入力が必要。v0.3 の自動パース補正は、この検出でホモグラフィが自動になった時点で実質達成

### v0.2.1（2026-09-06 追加）: 向きの自動補正とマーカー解読
- 実機テストで、フラットベッドが A4 を縦で読むため横向きページが 90° 回って入ってくることが分かった。`src/features/scan/orientation.ts`: QR の上辺の向きから 90° 単位の回転量を出し、取り込み時に画像を回してから検出する（`rotateBitmap`）。回した画像がそのスキャンの実体になる
- QR が読めないページは向きを当てられず上下逆になることがあった。`detectMarkersBlind` + `decodeMarker`: 画像の四隅に検索を張り、見つけた正方形を ArUco（MIP_36h12）として 4 方向で辞書照合（Hamming ≤ 4）。ID からページ番号と隅が分かるので、向きとページ番号を多数決で決める。QR ありの経路でも、別の隅として解読できた正方形は落書きとして捨てる
- 四隅指定に「自動検出をやり直す」「90° 回転」ボタン。疑似スキャンには `hideQr`（QR を塗りつぶす）オプション
- 未対応: マーカーの解読は外接矩形をそのままサンプリングするので、大きく傾いた写真ではデコードできない（QR が読めればその経路で問題ない）

### UI v2（2026-09-06）
- デザインハンドオフに沿って白黒・ピル型のトーンに刷新。トークンは `src/index.css` の `@theme`。ヘッダーのステッパーと下部の浮くドックで導線を集約
- 実機テスト後の調整: 切り出し済みページの案内は緑のチェックと「P n を切り出しました（m コマ）」を主文にし、調整のヒントは薄く従属させる。スキャン一覧はファイル名順ではなくページ順（未判定は末尾）で表示し、並びがそのままアニメーションの順になるようにする
- 隅の手動指定が必要なページで、QR が印刷時と違う象限にある（QR が読めないときは縦横比が合わない）場合は、隅を指定する前に「90° 回転」で向きを直すよう警告する（`checkOrientation`）。自動検出で隅が見つかっているページは向きに関係なく正しく切り出せるので警告しない
- 実機スキャン 4 枚（Brother フラットベッド、300 dpi、縦置き）で取り込みが止まる原因を修正: (a) 回転後に QR を読み直して失敗すると QR ありのページが「QR なし」に落ちていた → QR の四隅座標を回転で計算し直し、読み直しをやめた（`rotateQrCorners`）。(b) 先頭のファイルに QR が無いと設定が無いまま解析され、後で設定が決まっても放置されていた → 取り込みループの後に、ページ未確定のものを再解析する。(c) 再解析でもページが決まらない場合は取り込み時と同じく空きページを割り当てる（`firstUnusedPage`）。4 点そろってもページが無い場合は Page を選ぶよう案内する
- 取り込みの高速化（実機 4 枚・300 dpi、同条件で旧 23 s → 11 s（初回）、6〜7 s → 3〜5 s（2 回目以降）。マシン負荷でぶれるので目安）。計測の結果、マーカー検出自体は 20 ms 程度で、重かったのは (a) QR が読めないページでの全解像度 QR 再試行 (b) 設定確定前に読んだページの再解析 (c) 切り出し（ワーカー 1 本で直列、1920×1080 × 12 コマで約 0.8 s）の 3 つ。対策: (a) `readPageQr` を縮小パス（1600 px）と念入りパス（2600 px・原寸）に分け、念入りパスは「設定が無い」か「マーカーも 1 つも解読できない」ときだけ走らせる (b) 取り込み前に「設定探しパス」で先頭から順に縮小 QR だけ読み、最初に読めたページで設定を決めてから全ページを解析する（読んだ結果は `quickQr` として引き継ぐ）。一覧にはファイルを先に全部並べる (c) 切り出しはコマごとにスキャンの必要な窓だけを切り出して（`sourceWindow` / `translateHomography`）最大 4 本のワーカープールに配り、JPEG 化もワーカー内で行う。プールは取り込み開始時に先に起動する。dev では `window.__timings` に段階ごとの所要時間（`stopwatch`）が残る
- 未対応: 回転時の JPEG 再エンコード（1 枚 0.1〜0.3 s、画質もわずかに落ちる）。元ファイルを保持して表示と切り出し側で回転を折り込めば無くせる
- 縦動画: レイアウトは元から動画のアスペクト比で紙の向きを選ぶ（`chooseOrientation`）ので、1080×1920 も iPhone の回転メタデータ付き 1920×1080 も縦向きの紙・縦長のコマで通る（mediabunny が表示サイズと回転を適用）。ただしプリセットは列×行固定で、縦動画では 3x3 / 4x3 / 2x3 が同じ 40×71 mm に潰れていたため、`gridPresetsFor(dims)` で縦動画には 2x2 / 3x2 / 4x2 を出し、`coerceGridPreset` で列数を保ったまま差し替える（4x3 → 4x2、3x3 → 3x2）。QR には実際の列×行が入るのでスキャン側は変更なし。手入力フォームは 5 種すべてを列挙
- ロゴと favicon: 印刷コマと隅のマーカーをかたどった単色のマーク（`src/components/ui/Logo.tsx`、ヘッダーの語標の左）。favicon は同じモチーフの黒地版（`public/favicon.svg`、ダークなタブでも見える）と `apple-touch-icon.png`（180 px）。`index.html` に description / og / theme-color を追加。スキャフォールドの紫の placeholder は削除
- 公開準備: MIT の `LICENSE`、deploy.yml に `push: branches: [main]` を復帰、README に公開 URL と縦動画の説明。残りは GitHub 側の操作（リポジトリを public に、Settings → Pages の Source を GitHub Actions に）
